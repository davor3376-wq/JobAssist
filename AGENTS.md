# AGENTS.md
- **Tech Stack:** React 19, Tailwind v4, Vite.
- **Layout Rules:** Always use a 12-column CSS Grid.
- **Component Patterns:** Use Functional Components with JSDoc.
- **Strict Constraint:** Never use 'absolute' positioning for layout blocks.
- **Workflow:** After every file edit, run `npm run lint`. If it fails, fix it immediately.